<UserControl x:Class="PGI.Views.Stocks.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1100"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Ajouter une nouvelle catégorie -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="25" Margin="0,0,0,20"
                BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#CBD5E1" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="🏷️ Ajouter une nouvelle catégorie" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,15"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox x:Name="TxtNewCategory" 
                           Grid.Column="0"
                           Height="45" 
                           Padding="15,10"
                           BorderBrush="#E2E8F0" 
                           BorderThickness="1"
                           FontSize="14"
                           Margin="0,0,15,0"
                           Text="Nom de la nouvelle catégorie"
                           Foreground="#94A3B8"
                           GotFocus="TxtNewCategory_GotFocus"
                           LostFocus="TxtNewCategory_LostFocus"/>
                    
                    <Button Grid.Column="1"
                            Content="+ Ajouter"
                            Background="#669BBC" 
                            Foreground="White"
                            BorderThickness="0"
                            Padding="25,12"
                            FontWeight="SemiBold"
                            FontSize="14"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="8" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#003049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>
        
        <!-- Liste des catégories -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="0" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#CBD5E1" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="CategoriesListPanel" Margin="0"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>

