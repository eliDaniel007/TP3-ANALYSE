<UserControl x:Class="PGI.Views.Finances.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <TextBlock Text="📄 Génération de Rapports" 
                     FontSize="24" 
                     FontWeight="SemiBold" 
                     Foreground="#003049"
                     Margin="0,0,0,30"/>
            
            <!-- Section 1: Rapports sur demande -->
            <TextBlock Text="Rapports sur demande" FontSize="18" FontWeight="SemiBold" Foreground="#003049" Margin="0,0,0,20"/>
            
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Rapport de taxes -->
                <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="25" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="📊 Rapport de taxes (TPS/TVQ)" FontSize="16" FontWeight="SemiBold" Foreground="#003049" Margin="0,0,0,15"/>
                        <TextBlock Text="Générer un rapport détaillé des taxes perçues et à remettre au gouvernement." 
                                 FontSize="13" 
                                 Foreground="#64748B" 
                                 TextWrapping="Wrap"
                                 Margin="0,0,0,20"/>
                        
                        <TextBlock Text="Période" FontSize="13" Foreground="#64748B" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" 
                                     x:Name="TxtTaxDateDebut"
                                     Text="2025-01-01"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                            <TextBlock Grid.Column="1" Text="→" FontSize="16" Margin="10,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="2" 
                                     x:Name="TxtTaxDateFin"
                                     Text="2025-01-31"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                        </Grid>
                        
                        <StackPanel Orientation="Horizontal">
                            <Button Content="📥 Générer PDF" 
                                    Background="#DC2626" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Margin="0,0,10,0"
                                    Click="BtnGenerateTaxPDF_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#991B1B"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="📊 Générer CSV" 
                                    Background="#669BBC" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Click="BtnGenerateTaxCSV_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#003049"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Rapport des ventes -->
                <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="25">
                    <StackPanel>
                        <TextBlock Text="💰 Rapport des Ventes" FontSize="16" FontWeight="SemiBold" Foreground="#003049" Margin="0,0,0,15"/>
                        <TextBlock Text="Rapport complet des ventes réalisées sur une période donnée." 
                                 FontSize="13" 
                                 Foreground="#64748B" 
                                 TextWrapping="Wrap"
                                 Margin="0,0,0,20"/>
                        
                        <TextBlock Text="Période" FontSize="13" Foreground="#64748B" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Grid.Column="0" 
                                     x:Name="TxtSalesDateDebut"
                                     Text="2025-01-01"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                            <TextBlock Grid.Column="1" Text="→" FontSize="16" Margin="10,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="2" 
                                     x:Name="TxtSalesDateFin"
                                     Text="2025-01-31"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                        </Grid>
                        
                        <StackPanel Orientation="Horizontal">
                            <Button Content="📥 Générer PDF" 
                                    Background="#10B981" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Margin="0,0,10,0"
                                    Click="BtnGenerateSalesPDF_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#059669"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="📊 Générer CSV" 
                                    Background="#669BBC" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Click="BtnGenerateSalesCSV_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#003049"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Section 2: État Financier Simplifié -->
            <TextBlock Text="État Financier Simplifié" FontSize="18" FontWeight="SemiBold" Foreground="#003049" Margin="0,20,0,20"/>
            
            <Border Background="White" CornerRadius="10" Padding="30">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Sélection de période -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Période :" FontSize="14" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,0,20"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" 
                              x:Name="CmbPeriod"
                              Background="White"
                              BorderBrush="#E2E8F0"
                              BorderThickness="2"
                              Padding="12,10"
                              FontSize="14"
                              Margin="0,0,0,20"
                              SelectionChanged="CmbPeriod_SelectionChanged">
                        <ComboBoxItem Content="Mois dernier" IsSelected="True"/>
                        <ComboBoxItem Content="Trimestre en cours"/>
                        <ComboBoxItem Content="Année en cours"/>
                    </ComboBox>
                    
                    <!-- Indicateurs financiers -->
                    <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Background="#F8FAFC" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Ventes totales :" FontSize="14" Foreground="#64748B" VerticalAlignment="Center"/>
                                <TextBlock x:Name="TxtVentesTotales" Text="125 450,00 $" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Coût des produits vendus :" FontSize="14" Foreground="#64748B" VerticalAlignment="Center"/>
                                <TextBlock x:Name="TxtCoutProduits" Text="-82 600,00 $" FontSize="16" FontWeight="SemiBold" Foreground="#DC2626" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Background="#EEF2FF" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                        <Grid>
                            <TextBlock Text="PROFIT BRUT :" FontSize="16" FontWeight="Bold" Foreground="#003049" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtProfitBrut" Text="42 850,00 $" FontSize="18" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                    
                    <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Background="#F8FAFC" CornerRadius="8" Padding="20">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Dépenses (Salaires, Exploitation) :" FontSize="14" Foreground="#64748B" VerticalAlignment="Center"/>
                                <TextBlock x:Name="TxtDepenses" Text="-18 200,00 $" FontSize="16" FontWeight="SemiBold" Foreground="#DC2626" HorizontalAlignment="Right"/>
                            </Grid>
                            <Border Background="#F0FDF4" CornerRadius="8" Padding="15" Margin="0,10,0,0">
                                <Grid>
                                    <TextBlock Text="PROFIT NET :" FontSize="18" FontWeight="Bold" Foreground="#003049" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TxtProfitNet" Text="24 650,00 $" FontSize="22" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

