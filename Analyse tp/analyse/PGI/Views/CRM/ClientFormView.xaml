<UserControl x:Class="PGI.Views.CRM.ClientFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <StackPanel>
                <!-- En-tête -->
                <Grid Margin="0,0,0,25">
                    <StackPanel>
                        <TextBlock Text="📋 Fiche Client : Nouveau Client" 
                                 FontSize="24" 
                                 FontWeight="SemiBold" 
                                 Foreground="#003049"
                                 Margin="0,0,0,15"/>
                        
                        <!-- KPIs spécifiques au client -->
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#10B981" CornerRadius="12" Padding="10,5" Margin="0,0,10,0">
                                <TextBlock Text="🟢 Actif" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                            </Border>
                            <Border Background="#EEF2FF" CornerRadius="8" Padding="10,5" Margin="0,0,10,0">
                                <TextBlock Text="Score: 85%" FontSize="12" FontWeight="SemiBold" Foreground="#003049"/>
                            </Border>
                            <Border Background="#F0FDF4" CornerRadius="8" Padding="10,5" Margin="0,0,10,0">
                                <TextBlock Text="CA Total: 12 450 $" FontSize="12" FontWeight="SemiBold" Foreground="#10B981"/>
                            </Border>
                            <Border Background="#FEF3C7" CornerRadius="8" Padding="10,5">
                                <TextBlock Text="⭐ Satisfaction: 4.5/5" FontSize="12" FontWeight="SemiBold" Foreground="#F59E0B"/>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <Button Content="➕ Ajouter interaction" 
                                Background="#669BBC" 
                                Foreground="White"
                                BorderThickness="0"
                                Padding="15,10"
                                FontSize="13"
                                Cursor="Hand"
                                Margin="0,0,10,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#003049"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="💾 Enregistrer" 
                                Background="#10B981" 
                                Foreground="White"
                                BorderThickness="0"
                                Padding="15,10"
                                FontSize="13"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#059669"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- TabControl avec 3 onglets -->
                <TabControl Background="Transparent" BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TabItem">
                                        <Border x:Name="Border" Background="White" BorderBrush="#E2E8F0" BorderThickness="1,1,1,0" CornerRadius="8,8,0,0" Padding="20,12" Margin="0,0,5,0">
                                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#669BBC"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="False">
                                                <Setter Property="Foreground" Value="#64748B"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TabControl.Resources>
                    
                    <!-- Onglet 1 : Informations Client -->
                    <TabItem Header="📋 Informations Client">
                        <Border Background="White" CornerRadius="0,10,10,10" Padding="30" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="(Formulaire avec champs : Nom, Email, Téléphone, Adresse, Type, Statut)" 
                                         FontSize="14" 
                                         Foreground="#64748B"
                                         TextWrapping="Wrap"/>
                                <TextBlock Text="À implémenter avec la base de données" 
                                         FontSize="12" 
                                         Foreground="#94A3B8"
                                         Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                    </TabItem>
                    
                    <!-- Onglet 2 : Historique des Interactions -->
                    <TabItem Header="💬 Interactions &amp; Suivis">
                        <Border Background="White" CornerRadius="0,10,10,10" Padding="30" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="(Feed chronologique des interactions : Appels, Courriels, Ventes auto)" 
                                         FontSize="14" 
                                         Foreground="#64748B"
                                         TextWrapping="Wrap"/>
                                <TextBlock Text="À implémenter avec la base de données" 
                                         FontSize="12" 
                                         Foreground="#94A3B8"
                                         Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                    </TabItem>
                    
                    <!-- Onglet 3 : Historique des Ventes -->
                    <TabItem Header="💰 Historique des Ventes">
                        <Border Background="White" CornerRadius="0,10,10,10" Padding="30" BorderBrush="#E2E8F0" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="(Tableau lecture seule : N° Facture, Date, Montant, Statut)" 
                                         FontSize="14" 
                                         Foreground="#64748B"
                                         TextWrapping="Wrap"/>
                                <TextBlock Text="Données venant du module Finances" 
                                         FontSize="12" 
                                         Foreground="#94A3B8"
                                         Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                    </TabItem>
                </TabControl>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>

