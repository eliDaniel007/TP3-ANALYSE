<UserControl x:Class="PGI.Views.CRM.SatisfactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1200"
             Background="Transparent">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="⭐ Satisfaction Client" 
                   FontSize="24" 
                   FontWeight="SemiBold" 
                   Foreground="#003049"
                   Margin="0,0,0,20"/>

        <!-- KPI -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="25" Margin="0,0,0,30" BorderBrush="#E2E8F0" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="60" Height="60" CornerRadius="30" Background="#FEF3C7" Margin="0,0,20,0">
                    <TextBlock Text="★" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#F59E0B"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Note Moyenne Globale" FontSize="14" Foreground="#64748B"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                        <TextBlock x:Name="TxtMoyenne" Text="0.0" FontSize="36" FontWeight="Bold" Foreground="#003049" Margin="0,0,5,0"/>
                        <TextBlock Text="/ 5" FontSize="20" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="0,0,0,6"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Liste des avis -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Derniers avis reçus" FontSize="16" FontWeight="SemiBold" Margin="20,20,0,10" Foreground="#003049"/>
                
                <DataGrid x:Name="EvaluationsDataGrid" 
                          Grid.Row="1"
                          AutoGenerateColumns="False" 
                          RowHeight="50"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Background="Transparent"
                          BorderThickness="0"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          FontSize="13"
                          Margin="0,0,0,10">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="20,15"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E2E8F0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding DateFormate}" Width="120"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding NomClient}" Width="200"/>
                        <DataGridTemplateColumn Header="Note" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NoteEtoiles}" Foreground="#F59E0B" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Commentaire" Binding="{Binding Commentaire}" Width="*"/>
                        <DataGridTextColumn Header="Facture" Binding="{Binding NumeroFacture}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>