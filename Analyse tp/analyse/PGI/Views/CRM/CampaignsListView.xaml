<UserControl x:Class="PGI.Views.CRM.CampaignsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="30">
        <StackPanel>
            <Grid Margin="0,0,0,25">
                <TextBlock Text="📢 Liste des Campagnes Marketing" 
                         FontSize="24" 
                         FontWeight="SemiBold" 
                         Foreground="#003049"
                         VerticalAlignment="Center"/>
                <Button Content="➕ Créer une campagne" 
                        Background="#669BBC" 
                        Foreground="White"
                        BorderThickness="0"
                        Padding="20,12"
                        FontSize="14"
                        Cursor="Hand"
                        HorizontalAlignment="Right"
                        Click="BtnNewCampaign_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#003049"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <Border Background="White" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1">
                <DataGrid x:Name="CampaignsDataGrid" 
                          AutoGenerateColumns="False" 
                          RowHeight="50"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="Transparent"
                          BorderThickness="0"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          FontSize="13">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="20,15"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#E2E8F0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F8FAFC"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="20,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" 
                                                Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nom de la campagne" Binding="{Binding NomCampagne}" Width="*"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                        <DataGridTextColumn Header="Date début" Binding="{Binding DateDebutFormate}" Width="120"/>
                        <DataGridTextColumn Header="Date fin" Binding="{Binding DateFinFormate}" Width="120"/>
                        <DataGridTextColumn Header="Budget" Binding="{Binding BudgetFormate}" Width="120"/>
                        <DataGridTextColumn Header="Destinataires" Binding="{Binding NombreDestinataires}" Width="100"/>
                        <DataGridTextColumn Header="Réponses" Binding="{Binding NombreReponses}" Width="100"/>
                        <DataGridTextColumn Header="Taux" Binding="{Binding TauxParticipationFormate}" Width="100"/>
                        <DataGridTemplateColumn Header="Statut" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding StatutColor}" 
                                            CornerRadius="12" 
                                            Padding="8,4"
                                            HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Statut}" 
                                                 Foreground="White" 
                                                 FontSize="11" 
                                                 FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Actions" Width="190">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✏️" 
                                                Width="30" Height="30" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="Modifier"
                                                Margin="2"
                                                Click="BtnEdit_Click"/>
                                        <Button Content="📊" 
                                                Width="30" Height="30" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="Détails"
                                                Margin="2"
                                                Click="BtnDetails_Click"/>
                                        <Button Content="✅" 
                                                Width="30" Height="30" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="Clôturer"
                                                Foreground="#10B981"
                                                Margin="2"
                                                Click="BtnCloturer_Click"/>
                                        <Button Content="🗑️" 
                                                Width="30" Height="30" 
                                                Background="Transparent" 
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="Supprimer"
                                                Foreground="#EF4444"
                                                Margin="2"
                                                Click="BtnDelete_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

