<UserControl x:Class="PGI.Views.CRM.CampaignsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="30">
        <StackPanel>
            <Grid Margin="0,0,0,25">
                <TextBlock Text="📢 Liste des Campagnes Marketing" 
                         FontSize="24" 
                         FontWeight="SemiBold" 
                         Foreground="#003049"
                         VerticalAlignment="Center"/>
                <Button Content="➕ Créer une campagne" 
                        Background="#669BBC" 
                        Foreground="White"
                        BorderThickness="0"
                        Padding="20,12"
                        FontSize="14"
                        Cursor="Hand"
                        HorizontalAlignment="Right"
                        Click="BtnNewCampaign_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#003049"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <Border Background="White" CornerRadius="10" Padding="30">
                <TextBlock Text="(Tableau des campagnes : Nom, Date début, Date fin, Statut, Taux de réponse)" 
                         FontSize="14" 
                         Foreground="#64748B"
                         TextWrapping="Wrap"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

