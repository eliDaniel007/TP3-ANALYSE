<Window x:Class="PGI.Views.Finances.PurchaseReceptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Réceptionner une commande"
        Height="550"
        Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="TxtNumeroCommande" 
                       Text="📦 Réceptionner la commande" 
                       FontSize="22" 
                       FontWeight="SemiBold" 
                       Foreground="#003049"/>
            <TextBlock x:Name="TxtFournisseur" 
                       Text="Fournisseur" 
                       FontSize="14" 
                       Foreground="#64748B"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Info -->
        <Border Grid.Row="1" Background="#FEF3C7" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <TextBlock TextWrapping="Wrap" Foreground="#92400E">
                <Run Text="⚠️" FontSize="14"/>
                <Run Text=" Entrez la quantité réellement reçue pour chaque produit. Le stock sera automatiquement mis à jour." FontSize="13"/>
            </TextBlock>
        </Border>

        <!-- Liste des produits -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="0">
            <DataGrid x:Name="ProductsDataGrid" 
                      AutoGenerateColumns="False" 
                      RowHeight="50"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="Transparent"
                      BorderThickness="0"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      FontSize="13">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#64748B"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="12,10"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Produit" Binding="{Binding NomProduit}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Commandé" Binding="{Binding QuantiteCommandee}" Width="90" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Déjà reçu" Binding="{Binding QuantiteRecue}" Width="90" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Qté reçue maintenant" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding QuantiteARecevoir, UpdateSourceTrigger=PropertyChanged}" 
                                         Padding="8,5"
                                         HorizontalContentAlignment="Center"
                                         BorderBrush="#E2E8F0"
                                         BorderThickness="1"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Boutons -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="✅ Réceptionner" 
                        Background="#10B981" 
                        Foreground="White"
                        BorderThickness="0"
                        Padding="25,12"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Margin="0,0,10,0"
                        Click="BtnConfirm_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#059669"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Annuler" 
                        Background="#E2E8F0" 
                        Foreground="#64748B"
                        BorderThickness="0"
                        Padding="25,12"
                        FontSize="14"
                        Cursor="Hand"
                        Click="BtnCancel_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#CBD5E1"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

