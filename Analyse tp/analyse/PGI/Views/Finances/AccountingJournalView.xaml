<UserControl x:Class="PGI.Views.Finances.AccountingJournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="📒 Journal Général (Comptabilité)" 
                     FontSize="24" 
                     FontWeight="SemiBold" 
                     Foreground="#003049"
                     Margin="0,0,0,20"/>
            
            <TextBlock Text="Lecture seule • Toutes les transactions sont générées automatiquement" 
                     FontSize="13" 
                     Foreground="#64748B"
                     Margin="0,0,0,15"/>
            
            <!-- Filtres par période -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Date début :" FontSize="14" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker Grid.Column="1" 
                         x:Name="TxtDateDebut" 
                         Background="White"
                         BorderBrush="#E2E8F0"
                         BorderThickness="2"
                         Padding="10"
                         Margin="0,0,20,0"/>
                
                <TextBlock Grid.Column="2" Text="Date fin :" FontSize="14" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker Grid.Column="3" 
                         x:Name="TxtDateFin" 
                         Background="White"
                         BorderBrush="#E2E8F0"
                         BorderThickness="2"
                         Padding="10"
                         Margin="0,0,20,0"/>
                
                <Button Grid.Column="4" Content="🔍 Filtrer" 
                        Background="#669BBC" 
                        Foreground="White"
                        BorderThickness="0"
                        Padding="20,10"
                        FontSize="13"
                        Cursor="Hand"
                        HorizontalAlignment="Left"
                        Click="BtnFilter_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#003049"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </StackPanel>

        <!-- Tableau du journal -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1">
            <DataGrid x:Name="JournalDataGrid" 
                    AutoGenerateColumns="False" 
                    RowHeight="45"
                    HeadersVisibility="Column"
                    GridLinesVisibility="Horizontal"
                    Background="Transparent"
                    BorderThickness="0"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    FontSize="13">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#64748B"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                    <DataGridTextColumn Header="Transaction" Binding="{Binding Transaction}" Width="*"/>
                    <DataGridTextColumn Header="Compte" Binding="{Binding Compte}" Width="200"/>
                    <DataGridTextColumn Header="Débit" Binding="{Binding Debit}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#DC2626"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Crédit" Binding="{Binding Credit}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#10B981"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Totaux -->
        <Border Grid.Row="2" Background="#EEF2FF" CornerRadius="10" Padding="20" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="TOTAUX (Débit = Crédit)" FontSize="14" FontWeight="Bold" Foreground="#003049" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" x:Name="TxtTotalDebit" Text="0,00 $" FontSize="16" FontWeight="Bold" Foreground="#DC2626" TextAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" x:Name="TxtTotalCredit" Text="0,00 $" FontSize="16" FontWeight="Bold" Foreground="#10B981" TextAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>

