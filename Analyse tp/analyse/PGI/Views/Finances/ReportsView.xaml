<UserControl x:Class="PGI.Views.Finances.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <TextBlock Text="📄 Génération de Rapports" 
                     FontSize="24" 
                     FontWeight="SemiBold" 
                     Foreground="#003049"
                     Margin="0,0,0,30"/>
            
            <!-- Section 1: Rapports sur demande -->
            <TextBlock Text="Rapports sur demande" FontSize="18" FontWeight="SemiBold" Foreground="#003049" Margin="0,0,0,20"/>
            
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Rapport de taxes -->
                <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="25" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="📊 Rapport de taxes (TPS/TVQ)" FontSize="16" FontWeight="SemiBold" Foreground="#003049" Margin="0,0,0,15"/>
                        <TextBlock Text="Générer un rapport détaillé des taxes perçues et à remettre au gouvernement." 
                                 FontSize="13" 
                                 Foreground="#64748B" 
                                 TextWrapping="Wrap"
                                 Margin="0,0,0,20"/>
                        
                        <TextBlock Text="Période" FontSize="13" Foreground="#64748B" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <DatePicker Grid.Column="0" 
                                     x:Name="TxtTaxDateDebut"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                            <TextBlock Grid.Column="1" Text="→" FontSize="16" Margin="10,0" VerticalAlignment="Center"/>
                            <DatePicker Grid.Column="2" 
                                     x:Name="TxtTaxDateFin"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                        </Grid>
                        
                        <StackPanel Orientation="Horizontal">
                            <Button Content="📥 Générer PDF" 
                                    Background="#DC2626" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Margin="0,0,10,0"
                                    Click="BtnGenerateTaxPDF_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#991B1B"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="📊 Générer CSV" 
                                    Background="#669BBC" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Click="BtnGenerateTaxCSV_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#003049"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Rapport des ventes -->
                <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="25">
                    <StackPanel>
                        <TextBlock Text="💰 Rapport des Ventes" FontSize="16" FontWeight="SemiBold" Foreground="#003049" Margin="0,0,0,15"/>
                        <TextBlock Text="Rapport complet des ventes réalisées sur une période donnée." 
                                 FontSize="13" 
                                 Foreground="#64748B" 
                                 TextWrapping="Wrap"
                                 Margin="0,0,0,20"/>
                        
                        <TextBlock Text="Période" FontSize="13" Foreground="#64748B" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <DatePicker Grid.Column="0" 
                                     x:Name="TxtSalesDateDebut"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                            <TextBlock Grid.Column="1" Text="→" FontSize="16" Margin="10,0" VerticalAlignment="Center"/>
                            <DatePicker Grid.Column="2" 
                                     x:Name="TxtSalesDateFin"
                                     Background="White"
                                     BorderBrush="#E2E8F0"
                                     BorderThickness="2"
                                     Padding="10"/>
                        </Grid>
                        
                        <StackPanel Orientation="Horizontal">
                            <Button Content="📥 Générer PDF" 
                                    Background="#10B981" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Margin="0,0,10,0"
                                    Click="BtnGenerateSalesPDF_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#059669"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="📊 Générer CSV" 
                                    Background="#669BBC" 
                                    Foreground="White"
                                    BorderThickness="0"
                                    Padding="15,10"
                                    FontSize="13"
                                    Cursor="Hand"
                                    Click="BtnGenerateSalesCSV_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#003049"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Section 2: Paramètres Fiscaux -->
            <TextBlock Text="Paramètres Fiscaux" FontSize="18" FontWeight="SemiBold" Foreground="#003049" Margin="0,20,0,20"/>
            
            <Border Background="White" CornerRadius="10" Padding="25" Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="⚙️ Configuration des taux de taxes (TPS/TVQ)" 
                             FontSize="16" 
                             FontWeight="SemiBold" 
                             Foreground="#003049" 
                             Margin="0,0,0,15"/>
                    <TextBlock Text="Modifiez les taux de taxes applicables aux factures. Les modifications seront appliquées aux prochaines factures créées." 
                             FontSize="13" 
                             Foreground="#64748B" 
                             TextWrapping="Wrap"
                             Margin="0,0,0,20"/>
                    <Button Content="⚙️ Ouvrir les Paramètres Fiscaux" 
                            Background="#669BBC" 
                            Foreground="White"
                            BorderThickness="0"
                            Padding="20,12"
                            FontSize="14"
                            Cursor="Hand"
                            HorizontalAlignment="Left"
                            Click="BtnTaxSettings_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#003049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>

