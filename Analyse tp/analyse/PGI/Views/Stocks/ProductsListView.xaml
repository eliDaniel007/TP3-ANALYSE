<UserControl x:Class="PGI.Views.Stocks.ProductsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1100"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Barre de contrôles (recherche + filtres + bouton ajout) -->
        <Border Grid.Row="0" Background="Transparent" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <!-- Recherche -->
                    <Border Background="White" CornerRadius="8" Padding="12,8" Margin="0,0,12,0" Width="300"
                            BorderBrush="#E2E8F0" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox x:Name="TxtSearch" 
                                   BorderThickness="0"
                                   Background="Transparent"
                                   VerticalAlignment="Center"
                                   Text="Rechercher par nom ou SKU..." 
                                   Foreground="#94A3B8"
                                   Width="230"
                                   GotFocus="TxtSearch_GotFocus"
                                   LostFocus="TxtSearch_LostFocus"
                                   TextChanged="TxtSearch_TextChanged"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Filtre Catégorie -->
                    <ComboBox x:Name="CmbCategorie" 
                            Width="180" 
                            Height="40"
                            Margin="0,0,12,0"
                            BorderBrush="#E2E8F0"
                            BorderThickness="1"
                            Background="White"
                            Padding="10,8"
                            FontSize="13"
                            HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Center"
                            SelectionChanged="CmbCategorie_SelectionChanged">
                        <ComboBoxItem Content="Toutes les catégories" IsSelected="True"/>
                    </ComboBox>
                    
                    <!-- Filtre Fournisseur -->
                    <ComboBox x:Name="CmbFournisseur" 
                            Width="180" 
                            Height="40"
                            Margin="0,0,12,0"
                            BorderBrush="#E2E8F0"
                            BorderThickness="1"
                            Background="White"
                            Padding="10,8"
                            FontSize="13"
                            HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Center"
                            SelectionChanged="CmbFournisseur_SelectionChanged">
                        <ComboBoxItem Content="Tous les fournisseurs" IsSelected="True"/>
                    </ComboBox>
                    
                    <!-- Filtre Statut -->
                    <ComboBox x:Name="CmbStatut" 
                            Width="150" 
                            Height="40"
                            Margin="0,0,12,0"
                            BorderBrush="#E2E8F0"
                            BorderThickness="1"
                            Background="White"
                            Padding="10,8"
                            FontSize="13"
                            HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Center"
                            SelectionChanged="CmbStatut_SelectionChanged">
                        <ComboBoxItem Content="Tous les statuts" IsSelected="True"/>
                        <ComboBoxItem Content="Actif"/>
                        <ComboBoxItem Content="Inactif"/>
                        <ComboBoxItem Content="À commander"/>
                    </ComboBox>
                    
                </StackPanel>
                
                <!-- Bouton Réinitialiser et Ajouter -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <!-- Bouton Réinitialiser -->
                    <Button x:Name="BtnResetFilters"
                            Content="R"
                            Width="36"
                            Height="36"
                            Background="White"
                            Foreground="#64748B"
                            BorderThickness="1"
                            BorderBrush="#E2E8F0"
                            FontSize="16"
                            FontWeight="Medium"
                            Cursor="Hand"
                            ToolTip="Réinitialiser les filtres"
                            Margin="5,0,5,0"
                            Click="BtnResetFilters_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8" 
                                                    Padding="10,8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F8FAFC"/>
                                        <Setter Property="Foreground" Value="#003049"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                
                <!-- Bouton Ajouter -->
                    <Button Content="+ Ajouter un produit"
                        Background="#669BBC" 
                        Foreground="White"
                        BorderThickness="0"
                        Padding="20,12"
                        FontWeight="SemiBold"
                        FontSize="14"
                        Cursor="Hand"
                        Click="BtnAddProduct_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="8" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#003049"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Tableau des produits -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#CBD5E1" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
            </Border.Effect>
            <DataGrid x:Name="ProductsDataGrid"
                    AutoGenerateColumns="False" 
                    RowHeight="55"
                    HeadersVisibility="Column"
                    GridLinesVisibility="None"
                    Background="Transparent"
                    BorderThickness="0"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    FontSize="13"
                    Margin="0">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#64748B"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="20,15"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="20,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" 
                                            Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>

                    <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="150"/>
                    <DataGridTextColumn Header="Nom du produit" Binding="{Binding Nom}" Width="300"/>
                    <DataGridTextColumn Header="Catégorie" Binding="{Binding Categorie}" Width="220"/>
                    <DataGridTextColumn Header="Fournisseur" Binding="{Binding Fournisseur}" Width="220"/>
                    <DataGridTextColumn Header="Prix" Binding="{Binding Prix}" Width="100"/>
                    <DataGridTextColumn Header="Coût" Binding="{Binding Cout}" Width="100"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80"/>
                    <DataGridTextColumn Header="Seuil" Binding="{Binding Seuil}" Width="80"/>
                    <DataGridTemplateColumn Header="Statut" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding StatutColor}" 
                                        CornerRadius="12" 
                                        Padding="8,4"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Statut}" 
                                             Foreground="White" 
                                             FontSize="11" 
                                             FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Actions" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" 
                                            Width="30" Height="30" 
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            ToolTip="Modifier"
                                            Margin="2"
                                            Click="BtnEdit_Click"/>
                                    <Button Content="🗑️" 
                                            Width="30" Height="30" 
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            ToolTip="Supprimer"
                                            Foreground="#DC2626"
                                            Margin="2"
                                            Click="BtnDelete_Click"/>
                                    <Button x:Name="BtnToggleStatus" 
                                            Width="30" Height="30" 
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Margin="2"
                                            Click="BtnToggleStatus_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content" Value="👁️"/>
                                                <Setter Property="ToolTip" Value="Désactiver"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsInactif}" Value="True">
                                                        <Setter Property="Content" Value="🚫"/>
                                                        <Setter Property="ToolTip" Value="Réactiver"/>
                                                        <Setter Property="Foreground" Value="#94A3B8"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button Content="🕐" 
                                            Width="30" Height="30" 
                                            Background="Transparent" 
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            ToolTip="Historique"
                                            Margin="2"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

